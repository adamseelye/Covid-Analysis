# Assets
GET        /assets/*file                                controllers.Assets.versioned(path="/public", file: Asset)
GET        /                                            controllers.HomeController.index

### Spark call test ###
GET          /test/country/:country/:stat               controllers.Test.country(country: String, stat: String)
GET          /test/state/:country/:state/:stat          controllers.Test.state(country: String, state: String, stat: String)

### COUNTRY ###
GET          /country/view                              controllers.Country.view()
GET          /country/states/:country                   controllers.Country.states(country: String)