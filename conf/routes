# Assets
GET        /assets/*file                                controllers.Assets.versioned(path="/public", file: Asset)
GET        /                                            controllers.HomeController.index

### Spark call test ###
# NOTE: Tests probably don't work; Ironically, not tested.
GET          /test/overall/:stat                        controllers.Test.overall(stat: String)
GET          /test/country/:country/:stat               controllers.Test.country(country: String, stat: String)
GET          /test/state/:country/:state/:stat          controllers.Test.state(country: String, state: String, stat: String)

### COUNTRY ###
GET          /country/view/:column/:order               controllers.Country.view(column: String, order: String)
GET          /country/states/:country/:column/:order    controllers.Country.states(country: String, column: String, order: String)